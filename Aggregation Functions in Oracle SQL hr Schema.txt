SELECT COUNT(EMPLOYEE_ID) FROM EMPLOYEES;
SELECT COUNT(DEPARTMENT_ID) FROM DEPARTMENTS;
SELECT SUM(SALARY),SUM(COMMISSION_PCT) FROM EMPLOYEES;
SELECT AVG(SALARY) FROM EMPLOYEES;
SELECT AVG(MIN_SALARY),AVG(MAX_SALARY) FROM JOBS;
SELECT MAX(SALARY),MIN(SALARY) FROM EMPLOYEES;
SELECT MIN(MAX_SALARY), MAX(MAX_SALARY) FROM JOBS;
SELECT COUNT(EMPLOYEE_ID) FROM EMPLOYEES GROUP BY DEPARTMENT_ID;
SELECT AVG(MIN_SALARY), AVG(MAX_SALARY) FROM JOBS GROUP BY JOB_TITLE;
SELECT DEPARTMENT_ID FROM EMPLOYEES GROUP BY DEPARTMENT_ID HAVING COUNT(EMPLOYEE_ID)>5;
SELECT JOB_TITLE FROM JOBS GROUP BY JOB_TITLE HAVING AVG(MAX_SALARY)>5000;