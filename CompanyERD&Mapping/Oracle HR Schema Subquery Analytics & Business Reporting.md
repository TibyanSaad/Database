1\. Salary Analysis Using Subqueries

* SELECT \* FROM EMPLOYEES WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES);
* SELECT \* FROM EMPLOYEES WHERE SALARY < (SELECT MAX(SALARY) FROM EMPLOYEES);
* SELECT DISTINCT DEPARTMENT\_NAME FROM DEPARTMENTS D JOIN EMPLOYEES E ON E.DEPARTMENT\_ID=D.DEPARTMENT\_ID WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES);
* SELECT FIRST\_NAME, DEPARTMENT\_NAME, SALARY FROM EMPLOYEES E JOIN DEPARTMENTS D ON E.DEPARTMENT\_ID=D.DEPARTMENT\_ID WHERE SALARY = (SELECT MAX(SALARY) FROM EMPLOYEES E1 WHERE E1.DEPARTMENT\_ID = E.DEPARTMENT\_ID);



2\. Department \& Location Analysis

* SELECT FIRST\_NAME FROM EMPLOYEES

WHERE DEPARTMENT\_ID IN( SELECT DEPARTMENT\_ID FROM DEPARTMENTS

WHERE LOCATION\_ID IN ( SELECT LOCATION\_ID FROM LOCATIONS

WHERE COUNTRY\_ID IN (SELECT COUNTRY\_ID  FROM COUNTRIES

WHERE COUNTRY\_NAME= 'United States of America')));

* SELECT DEPARTMENT\_NAME FROM DEPARTMENTS

WHERE DEPARTMENT\_ID IN ( SELECT DEPARTMENT\_ID FROM EMPLOYEES);

* SELECT DEPARTMENT\_NAME FROM DEPARTMENTS

WHERE DEPARTMENT\_ID NOT IN (SELECT DISTINCT DEPARTMENT\_ID FROM EMPLOYEES

WHERE DEPARTMENT\_ID IS NOT NULL);

* SELECT FIRST\_NAME FROM EMPLOYEES

WHERE DEPARTMENT\_ID IN( SELECT DEPARTMENT\_ID FROM DEPARTMENTS

WHERE LOCATION\_ID IN ( SELECT LOCATION\_ID FROM LOCATIONS

WHERE COUNTRY\_ID IN (SELECT COUNTRY\_ID  FROM COUNTRIES

WHERE REGION\_ID IN (SELECT REGION\_ID FROM REGIONS WHERE REGION\_NAME = 'Europe'))));



3\. Manager \& Hierarchy Analysis

* SELECT FIRST\_NAME FROM EMPLOYEES E

WHERE SALARY > (SELECT SALARY FROM EMPLOYEES M WHERE  E.MANAGER\_ID = M.EMPLOYEE\_ID);

* SELECT FIRST\_NAME AS MANAGER\_NAME FROM EMPLOYEES M

WHERE 10000< ANY (SELECT SALARY FROM EMPLOYEES E WHERE E.MANAGER\_ID=M.EMPLOYEE\_ID);

* SELECT FIRST\_NAME FROM EMPLOYEES E

WHERE DEPARTMENT\_ID IN (SELECT DEPARTMENT\_ID FROM EMPLOYEES M

WHERE M.EMPLOYEE\_ID = E.MANAGER\_ID);

* SELECT FIRST\_NAME FROM EMPLOYEES E

WHERE DEPARTMENT\_ID NOT IN (SELECT DEPARTMENT\_ID FROM EMPLOYEES M

WHERE M.EMPLOYEE\_ID = E.MANAGER\_ID);



4\. Inline View \& Query Execution

* SELECT FIRST\_NAME FROM EMPLOYEES WHERE SALARY>10000;
* SELECT DEPARTMENT\_NAME FROM DEPARTMENTS

WHERE DEPARTMENT\_ID IN (SELECT DEPARTMENT\_ID FROM EMPLOYEES HAVING AVG(SALARY)>9000 GROUP BY DEPARTMENT\_ID);

* SELECT DEPARTMENT\_NAME FROM DEPARTMENTS

WHERE DEPARTMENT\_ID IN (SELECT DEPARTMENT\_ID FROM EMPLOYEES

HAVING COUNT(EMPLOYEE\_ID)>5 GROUP BY DEPARTMENT\_ID);



5\. Advanced Subquery Logic

* SELECT FIRST\_NAME FROM EMPLOYEES E

WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES M 

WHERE M.DEPARTMENT\_ID=E.DEPARTMENT\_ID);

* SELECT FIRST\_NAME FROM EMPLOYEES E

WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES M 

WHERE M.JOB\_ID=E.JOB\_ID);

* SELECT DEPARTMENT\_NAME FROM DEPARTMENTS

WHERE 5000 < ALL (SELECT SALARY FROM EMPLOYEES

WHERE DEPARTMENT\_ID = DEPARTMENTS.DEPARTMENT\_ID);

* SELECT FIRST\_NAME FROM EMPLOYEES 

WHERE DEPARTMENT\_ID IN (SELECT DEPARTMENT\_ID FROM EMPLOYEES 

HAVING COUNT(DEPARTMENT\_ID)=1 GROUP BY DEPARTMENT\_ID);



6\. Executive HR Analytics Challenge

* SELECT FIRST\_NAME FROM EMPLOYEES

WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES);

* SELECT DEPARTMENT\_NAME FROM DEPARTMENTS

WHERE DEPARTMENT\_ID IN (SELECT DEPARTMENT\_ID FROM EMPLOYEES

GROUP BY DEPARTMENT\_ID 

HAVING COUNT(EMPLOYEE\_ID) > (SELECT COUNT(EMPLOYEE\_ID) FROM EMPLOYEES 

WHERE DEPARTMENT\_ID =50));

* SELECT FIRST\_NAME FROM EMPLOYEES

WHERE DEPARTMENT\_ID = (SELECT DEPARTMENT\_ID FROM EMPLOYEES GROUP BY DEPARTMENT\_ID

HAVING AVG(SALARY) = (SELECT MAX(AVG(SALARY)) FROM EMPLOYEES GROUP BY DEPARTMENT\_ID));

* SELECT FIRST\_NAME, SALARY FROM EMPLOYEES

WHERE SALARY = (SELECT MIN(SALARY)FROM (

SELECT DISTINCT SALARY FROM EMPLOYEES

ORDER BY SALARY DESC)

WHERE ROWNUM <= 2);

* SELECT FIRST\_NAME FROM EMPLOYEES 

WHERE EMPLOYEE\_ID IN( SELECT MANAGER\_ID FROM EMPLOYEES  

GROUP BY MANAGER\_ID HAVING  AVG(SALARY)>10000);



























