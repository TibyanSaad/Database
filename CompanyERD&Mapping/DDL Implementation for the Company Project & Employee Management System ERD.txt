CREATE TABLE Employees (Ssn int PRIMARY KEY,Fname VARCHAR(50) NOT NULL,Lname VARCHAR(50) NOT NULL,Birth_Date DATE,
Gender VARCHAR(10) CHECK (Gender IN('Male', 'Female')));

CREATE TABLE DEPARTMENTS (Dnum int PRIMARY KEY, Dname VARCHAR(100), Locations VARCHAR(150), Hiring_Date DATE);
ALTER TABLE DEPARTMENTS ADD Ssn int;
ALTER TABLE DEPARTMENTS ADD CONSTRAINT fk_employee FOREIGN KEY (Ssn) REFERENCES EMPLOYEES(Ssn);

ALTER TABLE EMPLOYEES ADD Supervisor_id int;
ALTER TABLE EMPLOYEES ADD CONSTRAINT fk_supervisor FOREIGN KEY (SUPERVISOR_ID) REFERENCES EMPLOYEES(Ssn);
ALTER TABLE EMPLOYEES ADD CONSTRAINT fk_department FOREIGN KEY (DNUM) REFERENCES DEPARTMENTS(DNUM);
CREATE TABLE DEPENDENTS (DEPENDENT_NAME VARCHAR(100), Gender VARCHAR(10) CHECK (Gender IN('Male', 'Female')), BIRTH_DATE DATE);
ALTER TABLE DEPENDENTS ADD CONSTRAINT pk_depname PRIMARY KEY (DEPENDENT_NAME);
ALTER TABLE DEPENDENTS ADD SSN int;
ALTER TABLE DEPENDENTS ADD CONSTRAINT fk_ssn FOREIGN KEY (Ssn) REFERENCES EMPLOYEES(Ssn);
ALTER TABLE DEPENDENTS DROP CONSTRAINT pk_depname;
ALTER TABLE DEPENDENTS ADD CONSTRAINT pk_depnamessn PRIMARY KEY (DEPENDENT_NAME,SSN);
CREATE TABLE PROJECTS (Pname VARCHAR(100), PNUMBER INT PRIMARY KEY, LOCATION VARCHAR(50), CITY VARCHAR(50));
ALTER TABLE PROJECTS ADD DNUM INT;
ALTER TABLE PROJECTS ADD CONSTRAINT fk_pnum FOREIGN KEY (DNUM) REFERENCES DEPARTMENTS(DNUM);
CREATE TABLE WORKING_HOURS (SSN INT, PNUM INT, HOURS FLOAT);
ALTER TABLE WORKING_HOURS ADD CONSTRAINT fk_ssnhours FOREIGN KEY (SSN) REFERENCES EMPLOYEES(SSN);
ALTER TABLE WORKING_HOURS ADD CONSTRAINT fk_pnumhours FOREIGN KEY (PNUM) REFERENCES PROJECTS(PNUM);
ALTER TABLE WORKING_HOURS ADD CONSTRAINT fk_pnumhours FOREIGN KEY (PNUM) REFERENCES PROJECTS(PNUMBER);
ALTER TABLE WORKING_HOURS ADD CONSTRAINT pk_ssnpnum PRIMARY KEY (SSN,PNUM);
ALTER TABLE DEPT_LOCATIONS ADD CONSTRAINT fk_deptnum FOREIGN KEY (DNUM) REFERENCES DEPARTMENTS (DNUM);
ALTER TABLE DEPT_LOCATIONS ADD CONSTRAINT pk_deptnumlocations PRIMARY KEY (DNUM,LOCATION);
ALTER TABLE DEPT_LOCATIONS ADD CONSTRAINT pk_deptnumlocations PRIMARY KEY (DNUM,LOCATION);

SELECT table_name, constraint_name, column_name FROM user_cons_columns ORDER BY table_name;